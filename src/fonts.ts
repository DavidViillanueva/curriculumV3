import { Patrick_Hand, Bangers } from "next/font/google";

export const patrickHand = Patrick_Hand({
  subsets: ["latin"],
  weight: "400",
  variable: "--font-muppet",
});

export const bangers = Bangers({
  subsets: ["latin"],
  weight: "400",
  variable: "--font-lego",
});
